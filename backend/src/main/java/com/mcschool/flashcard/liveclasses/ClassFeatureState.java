package com.mcschool.flashcard.liveclasses;

/** Toggle state of an optional in-class feature (recording, transcription). */
public enum ClassFeatureState {
    INACTIVE,
    STARTING,
    ACTIVE,
    STOPPING,
    FAILED
}
