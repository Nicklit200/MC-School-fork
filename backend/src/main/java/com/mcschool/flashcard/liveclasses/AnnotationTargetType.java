package com.mcschool.flashcard.liveclasses;

/**
 * Surface an annotation document is attached to.
 *
 * <p>NOTEBOOK_CAMERA is reserved for the later phone-published notebook track so
 * that feature is an additional target value rather than a model redesign.
 * Nothing writes it in this release.
 */
public enum AnnotationTargetType {
    WHITEBOARD,
    SCREEN_SHARE,
    NOTEBOOK_CAMERA
}
